---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`About Us | ${SITE_TITLE}`} description="We build websites for tradespeople." />
	</head>
	<body>
		<Header />
		<main class="container page-content">
			<section class="section text-center">
				<h1>About Trade Site Builders</h1>
				<p class="lead">We’re on a mission to get good tradespeople the recognition (and work) they deserve.</p>
			</section>

			<section class="section split-layout">
				<div class="content">
					<h2>Our Story</h2>
					<p>
						It started simple. We noticed that many excellent builders, plumbers, and electricians were fantastic at their trade but had websites that looked like they were built in 1999.
					</p>
					<p>
						In today's world, if you don't look professional online, customers assume you aren't professional onsite. We changed that.
					</p>
					<p>
						We combine high-end design with local SEO strategies to ensure not only does your website look great, but it actually brings in leads.
					</p>
				</div>
				<div class="image-placeholder">
					<!-- Image of team or office -->
					<span>Team Photo / Workspace</span>
				</div>
			</section>

			<section class="section values">
				<h2>Our Values</h2>
				<div class="grid-3">
					<div class="value-card">
						<h3>No Nonsense</h3>
						<p>We don't use jargon. We explain everything in plain English.</p>
					</div>
					<div class="value-card">
						<h3>Speed</h3>
						<p>We build fast. Your site can be live in as little as 2 weeks.</p>
					</div>
					<div class="value-card">
						<h3>Results</h3>
						<p>We focus on one thing: getting you more enquiries.</p>
					</div>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.page-content {
		padding-top: 100px;
	}
	.lead {
		font-size: 1.5rem;
		color: var(--color-text-muted);
		max-width: 800px;
		margin: 0 auto;
	}
	.split-layout {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 3rem;
		align-items: center;
	}
	.image-placeholder {
		background: var(--color-bg-offset);
		height: 300px;
		display: flex;
		align-items: center;
		justify-content: center;
		border-radius: 1rem;
		border: 1px solid var(--color-border);
	}
	.grid-3 {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
		margin-top: 2rem;
	}
	.value-card {
		padding: 2rem;
		background: var(--color-bg-offset);
		border-radius: 8px;
		border: 1px solid var(--color-border);
	}
	h3 { color: var(--color-primary); }
	
	@media (max-width: 768px) {
		.split-layout { grid-template-columns: 1fr; }
	}
</style>
